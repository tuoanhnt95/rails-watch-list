<%= render 'shared/navbar' %>

<div class="container">
  <h3>Add list</h3>

  <%= simple_form_for [@list,@bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie, collection: @movies %>
    <%= f.submit %>
  <% end %>
</div>
